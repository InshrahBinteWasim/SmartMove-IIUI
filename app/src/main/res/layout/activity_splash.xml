<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Islamic Pattern Watermark -->
    <ImageView
        android:id="@+id/img_pattern"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.08"
        android:scaleType="center"
        android:src="@drawable/bg_islamic_pattern" />

    <!-- Top Left Blinking Dot (System Status) -->
    <View
        android:id="@+id/view_status_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_margin="24dp"
        android:background="@drawable/splash_dot_bright"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="@string/splash_system_status"
        android:textColor="@color/primary"
        android:textSize="10sp"
        app:layout_constraintBottom_toBottomOf="@+id/view_status_dot"
        app:layout_constraintStart_toEndOf="@+id/view_status_dot"
        app:layout_constraintTop_toTopOf="@+id/view_status_dot" />

    <!-- Building Illustration -->
    <ImageView
        android:id="@+id/img_building"
        android:layout_width="320dp"
        android:layout_height="220dp"
        android:layout_marginTop="40dp"
        android:src="@drawable/ic_building_iiui"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Green Bus -->
    <ImageView
        android:id="@+id/img_bus"
        android:layout_width="280dp"
        android:layout_height="140dp"
        android:src="@drawable/ic_bus_green"
        app:layout_constraintBottom_toBottomOf="@+id/img_building"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/img_building"
        app:layout_constraintVertical_bias="0.2" />

    <!-- App Name & Department -->
    <LinearLayout
        android:id="@+id/layout_branding"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/img_bus">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="serif"
            android:text="SmartMove IIUI"
            android:textColor="@color/text_primary"
            android:textSize="32sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:letterSpacing="0.1"
            android:text="@string/splash_dept_name"
            android:textColor="@color/primary"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Department Icons Circle -->
    <ImageView
        android:id="@+id/img_dept_icons"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/ic_dept_icons_circle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layout_branding" />

    <!-- Bottom Tagline -->
    <TextView
        android:id="@+id/tv_tagline"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="@string/splash_tagline_full"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/img_dept_icons" />

    <!-- Skyline Silhouette at Bottom -->
    <ImageView
        android:id="@+id/img_skyline"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:alpha="0.5"
        android:scaleType="fitXY"
        android:src="@drawable/splash_skyline_city"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Small Bus on Skyline -->
    <ImageView
        android:id="@+id/img_mini_bus"
        android:layout_width="60dp"
        android:layout_height="30dp"
        android:layout_marginBottom="12dp"
        android:src="@drawable/ic_splash_mini_bus"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Progress Indicator -->
    <ProgressBar
        android:id="@+id/splash_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="160dp"
        android:layout_height="4dp"
        android:layout_marginBottom="40dp"
        android:progressDrawable="@drawable/splash_progress_track"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
